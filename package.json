{
  "name": "ai-notes-monorepo",
  "private": true,
  "version": "0.1.0",
  "workspaces": [
    "backend",
    "frontend"
  ],
  "scripts": {
    "dev": "npm run dev --workspace backend",
    "dev:backend": "npm run start:dev --workspace backend",
    "dev:frontend": "npm run dev --workspace frontend",
    "build": "npm run build --workspace backend && npm run build --workspace frontend",
    "lint": "npm run lint --workspace backend && npm run lint --workspace frontend",
    "test:e2e": "docker compose up -d db && npm run prisma:generate --workspace backend && npm run prisma:deploy --workspace backend && playwright test",
    "test:e2e:headed": "playwright test --headed",
    "format": "npm run format --workspace backend && npm run format --workspace frontend",
    "prisma:generate": "npm run prisma:generate --workspace backend",
    "prisma:migrate": "npm run prisma:migrate --workspace backend"
  },
  "devDependencies": {
    "@playwright/test": "^1.54.2"
  }
}
